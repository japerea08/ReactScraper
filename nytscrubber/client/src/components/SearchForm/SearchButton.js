import React from "react";
import "./SearchForm.css";

export const SearchButton = props => (
  <button id="searchButton" {...props}className="btn btn-success">
  Search
  </button>
);

export default SearchButton;
